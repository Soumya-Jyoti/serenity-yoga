PORT=5000

# Comma-separated list of origins allowed to call this API. Must include
# the deployed frontend's domain — see docs/HOSTINGER-DEPLOY.md.
CORS_ORIGINS=http://localhost:5173
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=
TOKEN_SECRET=change_this_to_a_long_random_string
